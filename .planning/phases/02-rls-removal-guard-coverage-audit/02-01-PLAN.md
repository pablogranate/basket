---
phase: 02-rls-removal-guard-coverage-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .github/workflows/ci.yml
  - vitest.config.mts
  - src/lib/auth.ts
  - src/lib/api/with-auth.ts
  - src/lib/api/rate-limit.ts
  - src/lib/settings.ts
  - src/lib/api/__tests__/with-auth.test.ts
  - src/lib/__tests__/settings-secret.test.ts
  - src/test/fixtures/user-context.ts
autonomous: false
requirements: [AUTHZ-01]
user_setup: []

must_haves:
  truths:
    - "An API route wrapped in withAuth returns 401 when getUserContext yields no session"
    - "An API route wrapped in withAuth returns 403 when the resolved role is not in the allowed set"
    - "withAuth passes the resolved UserContext to the inner handler on the success path"
    - "The user-facing settings surface never exposes the raw app_settings.secret_value to a non-admin (D-08), while the server-only runtime read (getGeminiRuntimeConfig) still returns a working key for guest/non-admin AI calls"
    - "vitest run executes and exits 0 on the new test files"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Vitest config with tsconfigPaths + react plugins, node environment"
      contains: "tsconfigPaths"
    - path: "src/lib/api/with-auth.ts"
      provides: "withAuth higher-order route wrapper (D-04/D-05)"
      exports: ["withAuth"]
    - path: "src/lib/api/rate-limit.ts"
      provides: "Guest AI rate limiter (D-05)"
      exports: ["consumeGuestRateLimit"]
    - path: "src/lib/auth.ts"
      provides: "Exported UserContext type seam for loaders + tests"
      contains: "export type UserContext"
    - path: "src/lib/settings.ts"
      provides: "User-facing secret_value redaction on the settings snapshot path (D-08); getGeminiRuntimeConfig runtime read stays ungated"
      contains: "secret_value"
    - path: "src/lib/api/__tests__/with-auth.test.ts"
      provides: "401/403/ctx-passthrough unit tests"
    - path: "src/lib/__tests__/settings-secret.test.ts"
      provides: "Non-admin secret-read denial test on the user-facing path + non-admin runtime-read-still-works test (D-08)"
    - path: "src/test/fixtures/user-context.ts"
      provides: "makeUserContext factory + getUserContext mock helper"
      exports: ["makeUserContext"]
  key_links:
    - from: "src/lib/api/with-auth.ts"
      to: "src/lib/auth.ts getUserContext"
      via: "import + await getUserContext()"
      pattern: "getUserContext"
    - from: "src/lib/api/__tests__/with-auth.test.ts"
      to: "src/lib/auth.ts"
      via: "vi.mock"
      pattern: "vi\\.mock\\(.@/lib/auth.\\)"
---

<objective>
Lay the Wave 0 test foundation and build the two interface primitives the rest of the phase consumes: the `withAuth` route higher-order wrapper (D-04/D-05) and the user-facing-only `secret_value` redaction (D-08). Export the `UserContext` type from `auth.ts` so Wave 2 loaders and route handlers depend on a shared contract rather than re-deriving it.

Purpose: This is the contract-first plan. `withAuth`, the exported `UserContext` type, the rate limiter, and the test harness must exist before any route/loader work in Wave 2. Without them, Wave 2 plans would each invent their own seam (scavenger-hunt anti-pattern).
Output: Vitest installed + configured + wired into `check`/CI; `withAuth`; `rate-limit`; exported `UserContext`; user-facing secret redaction; the auth-context mock fixture; and the first two passing test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (downstream drift verification must exclude them as newly-created, not pre-existing):
- `withAuth` (`src/lib/api/with-auth.ts`)
- `consumeGuestRateLimit` (`src/lib/api/rate-limit.ts`)
- `UserContext` exported type (`src/lib/auth.ts`)
- `makeUserContext` test factory (`src/test/fixtures/user-context.ts`)
- `vitest.config.mts`
- `"test"` script in `package.json`
- A user-facing secret-redaction change on the `secret_value` surface in `src/lib/settings.ts` (the runtime read `getGeminiRuntimeConfig` is NOT gated)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify rate-limiter-flexible package legitimacy (D-05, [ASSUMED])</name>
  <read_first>
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Package Legitimacy Audit, lines 104-116; Open Question 3, lines 467-468)
  </read_first>
  <what-built>Nothing yet — this is a pre-install legitimacy gate. RESEARCH flagged `rate-limiter-flexible@^11.1.0` as [ASSUMED] because slopcheck was unavailable. The three dev deps (`vitest`, `@vitejs/plugin-react`, `vite-tsconfig-paths`) are verbatim from the official Next.js Vitest guide and are NOT gated.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/rate-limiter-flexible — confirm it is the `animir/node-rate-limiter-flexible` package, has substantial weekly downloads, and a recent publish.
    2. Confirm version ^11.1.0 exists.
    3. Decide: approve `rate-limiter-flexible` OR instruct the executor to descope to a hand-rolled in-memory `Map`-based limiter (acceptable per RESEARCH A1 for a single-instance internal tool).
    4. Also confirm the rate-limit BACKING STORE choice: in-memory is fine for the bounded migration window; Netlify may be multi-instance (Open Q3) — confirm in-memory is acceptable or request durable store.
  </how-to-verify>
  <acceptance_criteria>
    - Operator records one of: "approved: rate-limiter-flexible, in-memory store" OR "descope: hand-rolled Map limiter, in-memory store".
    - If durable store is requested, plan is paused and escalated (out-of-scope for this phase per D-01 "don't re-architect").
  </acceptance_criteria>
  <resume-signal>Type "approved" (with store choice) or "descope to Map"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Vitest + rate limiter, configure runner, wire into check + CI (Wave 0)</name>
  <read_first>
    - package.json (current scripts: line 11 `check`)
    - .github/workflows/ci.yml (steps: Lint/Typecheck/Build, lines 27-34)
    - eslint.config.mjs (ESM default-export config style precedent)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Validation Architecture, lines 485-526; install commands lines 90-102)
  </read_first>
  <action>
    Run `npm install -D vitest@^4.1.8 @vitejs/plugin-react@^6.0.2 vite-tsconfig-paths@^6.1.1`. Then install the rate limiter per Task 1's decision: either `npm install rate-limiter-flexible@^11.1.0` OR skip if descoped to a `Map`. Verify versions first with `npm view vitest version` etc.
    Create `vitest.config.mts` as an ESM default-export `defineConfig` from `vitest/config` with `plugins: [tsconfigPaths(), react()]` and `test: { environment: "node" }` (node env — no DOM needed for route/loader/static-analysis tests per RESEARCH line 497). Import `tsconfigPaths` from `vite-tsconfig-paths` and `react` from `@vitejs/plugin-react`.
    Add `"test": "vitest run"` to `package.json` scripts and fold it into `check`: `check` becomes `npm run lint && npm run typecheck && npm run test && npm run build`.
    Add a `Test` step to `.github/workflows/ci.yml` AFTER the Typecheck step and BEFORE Build, mirroring the existing `run: npm run ...` step format: `- name: Test` / `run: npm run test`.
    Commit only `package-lock.json` (CI uses `npm ci`); do NOT touch `pnpm-lock.yaml`.
  </action>
  <verify>
    <automated>npx vitest --version && grep -q '"test": "vitest run"' package.json && grep -q 'npm run test' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.mts` exists and contains `tsconfigPaths` and `react` plugin references.
    - `package.json` `scripts.test` equals `vitest run`; `scripts.check` includes `npm run test`.
    - `.github/workflows/ci.yml` contains a `Test` step running `npm run test` placed between Typecheck and Build.
    - `npx vitest --version` prints a 4.x version.
  </acceptance_criteria>
  <done>Vitest runs locally, `npm run test` is in `check` and CI, lockfile updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build withAuth HOF + rate limiter + UserContext type + auth-context mock fixture</name>
  <read_first>
    - src/lib/auth.ts (getUserContext lines 28-104, requireUserContext, requireEditor; the return shape to type as UserContext)
    - src/app/api/team-logo/route.ts (the getUserContext→401 pattern withAuth generalizes — analog per 02-PATTERNS.md lines 33-61)
    - src/lib/database.types.ts (AppRole type for the roles option)
    - src/lib/supabase/middleware.ts (ALLOW_GUEST_MI_JORNADA whitelist ~lines 68-72 — guest source of truth for D-05)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pattern 1 sketch lines 170-226; mocking strategy line 510)
  </read_first>
  <behavior>
    - withAuth({}, handler): getUserContext returns userId=null and allowGuest absent → response status 401, body has `error`.
    - withAuth({ allowGuest: true }, handler): userId=null → inner handler is called with the guest context (no 401).
    - withAuth({ roles: ["admin"] }, handler): resolved role "collaborator" → response status 403.
    - withAuth({ roles: ["admin","editor"] }, handler): resolved role "editor" → inner handler called, receives the resolved ctx (assert ctx.userId/role pass-through).
    - consumeGuestRateLimit(key): allows the first N calls, then returns a blocked/false result on the (N+1)th within the window.
  </behavior>
  <action>
    In `src/lib/auth.ts` add `export type UserContext = Awaited<ReturnType<typeof getUserContext>>;` (the type seam; do not change runtime behavior).
    Create `src/lib/api/with-auth.ts` exporting `function withAuth(options, handler)`. Options shape: `{ roles?: ReadonlyArray<AppRole>; allowGuest?: boolean }`. Behavior: `await getUserContext()`; if `!context.userId` and `!options.allowGuest` → `NextResponse.json({ error: "Tu sesión no está activa para usar este endpoint." }, { status: 401 })`; if `!context.userId` and `allowGuest` → call handler with guest context; if `options.roles` set and `!options.roles.includes(context.role)` → `NextResponse.json({ error: "No tenes permisos para usar este endpoint." }, { status: 403 })`; else call `handler(request, context)`. Reuse `context.role` (already runs `resolveDashboardAccessRole`) — never re-derive role. Use `import type { AppRole }` and `import type { UserContext }`. Bracketed `[with-auth]` console prefix on any error path.
    Create `src/lib/api/rate-limit.ts` exporting `consumeGuestRateLimit(key: string)` using `rate-limiter-flexible` in-memory `RateLimiterMemory` (or a `Map`-based sliding window if descoped). Cap: a small per-key limit (e.g. points per duration) suitable for guest AI; return a boolean/result the route can check. Bracketed `[rate-limit]` prefix.
    Create `src/test/fixtures/user-context.ts` exporting `makeUserContext(overrides?: Partial<UserContext>): UserContext` returning a sensible authenticated default (userId set, role "editor", canEdit true) merged with overrides; export a `mockGetUserContext` helper or document the `vi.mock("@/lib/auth")` pattern callers use.
    Create `src/lib/api/__tests__/with-auth.test.ts` covering the five behaviors above, mocking `@/lib/auth` getUserContext via `vi.mock` + `makeUserContext`. Call the wrapped handler directly with a constructed `new Request(...)` (no NTARH) and assert `.status` / parsed JSON.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/__tests__/with-auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/api/with-auth.ts` contains `export function withAuth`.
    - `src/lib/auth.ts` contains `export type UserContext`.
    - `src/lib/api/rate-limit.ts` exports `consumeGuestRateLimit`.
    - `npx vitest run src/lib/api/__tests__/with-auth.test.ts` exits 0 with the five behavior cases green (401 no-session, guest pass, 403 under-priv, ctx pass-through, rate-limit block).
  </acceptance_criteria>
  <done>withAuth enforces 401/403/guest correctly and passes ctx through; rate limiter blocks after N; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Redact app_settings.secret_value on the USER-FACING path only (D-08) + denial test</name>
  <read_first>
    - src/lib/settings.ts (getPortalGeminiConfig :50-86 reads `secret_value`; getGeminiRuntimeConfig :88-146 = the SERVER-ONLY RUNTIME read that MUST stay functional for guest/non-admin AI; getSettingsSnapshot :148-163 = the USER-FACING surface that already returns ONLY booleans like `hasPortalGeminiKey`, never the raw secret)
    - src/app/(dashboard)/settings/page.tsx (:36 calls getSettingsSnapshot — the admin settings UI; the surface a user could read)
    - src/app/actions/settings.ts (:83 admin-only write of `secret_value`; :92 the upsert — confirms admin-gated WRITE already exists)
    - src/lib/auth.ts (UserContext / role — only used to gate the user-facing exposure, NOT the runtime read)
    - .planning/codebase/CONCERNS.md (CRITICAL: secret_value global read, lines 39-43)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (D-08 line 18; settings-secret test row line 507)
  </read_first>
  <behavior>
    - The user-facing settings surface (`getSettingsSnapshot` / settings UI) NEVER returns the raw `secret_value` to any caller — only presence booleans / a masked form. (It already returns only booleans today — this task locks that with a test and adds masking if any raw value is ever surfaced.)
    - `getGeminiRuntimeConfig` (server-only RUNTIME read) STILL returns the actual portal `apiKey` for a non-admin / guest caller — so guest-allowed AI routes (`ai/metric-capture`, `ai/section` under D-05) keep working. It does NOT call `getUserContext()` and is NOT role-gated.
    - The ~8 dashboard pages that call `getSettingsSnapshot` keep working for all roles (booleans only).
  </behavior>
  <action>
    D-08 INTENT (per checker WARNING): deny only the USER-FACING / PostgREST-style read of the raw `secret_value`; keep the server-only RUNTIME read functional. The shared low-level helper `getPortalGeminiConfig()` is consumed by BOTH `getGeminiRuntimeConfig()` (legitimate server runtime, guest-allowed) AND `getSettingsSnapshot()` (user-facing). DO NOT gate `getPortalGeminiConfig`/`getGeminiRuntimeConfig` by `context.role === 'admin'` — that would strip the portal Gemini key from guest/non-admin AI calls and regress the guest mi-jornada AI feature (D-05). DO NOT call `getUserContext()` inside the runtime helper.
    Instead, enforce D-08 on the USER-FACING surface only:
    - Confirm `getSettingsSnapshot()` returns ONLY presence booleans (`hasPortalGeminiKey`, etc.) and never the raw `secret_value` — it already does. Keep it that way; if any future field would surface a raw key, route it through `maskApiKey` (:165). Add an explicit inline comment at the snapshot boundary: "user-facing — never return raw secret_value (D-08)".
    - The admin-only WRITE of `secret_value` (`src/app/actions/settings.ts` :83 `user.role === "admin"` guard around the upsert) already exists — verify it; this is the only admin-gated touchpoint for the secret. Do not duplicate it into the read helper.
    - The app-layer mitigation that matters once RLS drops: ensure NO user-facing code path (snapshot, action return, page prop) ever ships the raw `secret_value` to the client. The direct-PostgREST exposure of the column is the accepted bounded window (D-01) closed at cutover; the app's job is to never leak it itself.
    Keep the `isMissingAppSettingsError` 42P01 handling and the `[settings]` bracketed prefix. Do NOT log the secret value.
    Create `src/lib/__tests__/settings-secret.test.ts` asserting: (1) `getSettingsSnapshot()` output contains NO raw `secret_value` for any role (admin or non-admin) — only booleans/masked; (2) `getGeminiRuntimeConfig()` STILL returns a non-empty `apiKey` from the portal source for a NON-ADMIN context (mock a row with a `secret_value` and a non-admin/guest context) — proving the runtime read is NOT gated and guest AI keeps working. Mock the supabase client read and `next/headers` cookies; do not mock `@/lib/auth` for the runtime-read assertion (it must not depend on role).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/settings-secret.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getSettingsSnapshot()` never returns the raw `secret_value` for any role (test asserts booleans/masked only).
    - `getGeminiRuntimeConfig()` still returns a working portal `apiKey` for a non-admin/guest context (test asserts non-empty apiKey) — no role gate, no getUserContext call inside it.
    - The admin-only WRITE guard in `src/app/actions/settings.ts` is verified present (unchanged).
    - `npx vitest run src/lib/__tests__/settings-secret.test.ts` exits 0.
    - No test or log statement prints the secret value.
  </acceptance_criteria>
  <done>The CRITICAL CONCERNS leak is closed at the app layer on the user-facing surface (D-08) WITHOUT breaking the guest/non-admin runtime Gemini path (D-05); proven by a passing test that checks both halves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/guest → API route handler | Untrusted session (or none) crosses into `api/*`; withAuth is the enforcement seam. |
| app server → user-facing settings surface | The raw `app_settings.secret_value` must never cross from server to client (snapshot/page prop); the runtime read stays server-only and functional. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `app_settings.secret_value` (Gemini key) on the user-facing surface | mitigate | Task 4 keeps `getSettingsSnapshot` boolean-only (D-08) + admin-only WRITE (existing) + a test proving the raw secret never reaches a user-facing return; runtime read stays ungated so guest AI (D-05) keeps working. Closes CONCERNS CRITICAL at the app layer. |
| T-02-02 | Elevation of Privilege | `api/*` route handlers (no per-route role gate today) | mitigate | Task 3 `withAuth` HOF returns 401/403 centrally (D-04); reused by Wave 2 routes. |
| T-02-03 | Denial of Service / financial | Guest-reachable paid Gemini routes | mitigate | Task 3 `consumeGuestRateLimit` (D-05); applied to guest routes in plan 02. |
| T-02-SC | Tampering | npm install of `rate-limiter-flexible` ([ASSUMED]) | mitigate | Task 1 blocking-human checkpoint verifies via npmjs.com before install; descope to Map if rejected. |
</threat_model>

<verification>
- `npm run test` runs Vitest and exits 0.
- `with-auth.test.ts` green: 401 no-session, guest pass, 403 under-priv, ctx pass-through, rate-limit block.
- `settings-secret.test.ts` green: snapshot exposes no raw secret for any role; runtime read still returns a working key for a non-admin/guest.
- `src/lib/auth.ts` exports `UserContext`; `src/lib/api/with-auth.ts` exports `withAuth`.
</verification>

<success_criteria>
- Vitest installed, configured (`vitest.config.mts`), wired into `check` + CI.
- `withAuth` enforces 401/403/guest and passes resolved ctx (D-04/D-05 seam ready for Wave 2).
- `UserContext` type exported from `auth.ts` (shared contract for loaders + tests).
- Rate limiter available (D-05).
- `secret_value` never leaks on the user-facing path (D-08) AND the server-only runtime read stays functional for guest/non-admin AI (D-05) — both proven by tests.
- Auth-context mock fixture available for all downstream route/loader tests.
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-01-SUMMARY.md` when done. Explicitly note that D-08 is enforced on the user-facing surface only and that `getGeminiRuntimeConfig` remains ungated so guest AI (D-05) is not regressed.
</output>
</content>
</invoke>
