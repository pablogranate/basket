---
phase: 02-rls-removal-guard-coverage-audit
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/api/ai/people/route.ts
  - src/app/api/ai/section/route.ts
  - src/app/api/ai/metric-capture/route.ts
  - src/app/api/ai/speedtest/route.ts
  - src/app/api/matches/intake/route.ts
  - src/app/api/ai/people/__tests__/auth.test.ts
  - src/app/api/ai/section/__tests__/auth.test.ts
  - src/app/api/ai/metric-capture/__tests__/auth.test.ts
  - src/app/api/ai/speedtest/__tests__/auth.test.ts
autonomous: false
requirements: [AUTHZ-01]
user_setup: []

must_haves:
  truths:
    - "Each api/ai/* route returns 401 when called with no session (non-guest routes)"
    - "Each non-guest api/ai/* route returns 403 for an under-privileged role"
    - "Guest-allowed AI routes (metric-capture, section) accept guests but are rate-limited"
    - "matches/intake enforces a machine-auth check (API key/shared secret), not silently open"
  artifacts:
    - path: "src/app/api/ai/people/route.ts"
      provides: "withAuth-wrapped POST (no guest)"
      contains: "withAuth("
    - path: "src/app/api/ai/section/route.ts"
      provides: "withAuth-wrapped POST (guest + rate limit)"
      contains: "withAuth("
    - path: "src/app/api/ai/metric-capture/route.ts"
      provides: "withAuth-wrapped POST (guest + rate limit)"
      contains: "withAuth("
    - path: "src/app/api/ai/speedtest/route.ts"
      provides: "withAuth-wrapped handler"
      contains: "withAuth("
    - path: "src/app/api/matches/intake/route.ts"
      provides: "Machine-auth-guarded intake (withApiKey or withAuth secret mode)"
      contains: "withAuth"
  key_links:
    - from: "src/app/api/ai/people/route.ts"
      to: "src/lib/api/with-auth.ts"
      via: "import withAuth"
      pattern: "withAuth"
    - from: "src/app/api/ai/metric-capture/route.ts"
      to: "src/lib/api/rate-limit.ts"
      via: "consumeGuestRateLimit on guest path"
      pattern: "consumeGuestRateLimit|RateLimit"
---

<objective>
Close the HIGH-severity unguarded-route gap: wrap every `api/ai/*` route in `withAuth` (D-04), apply rate limiting to the guest-allowed AI routes (D-05), and resolve the machine-route auth for `matches/intake` (Open Q1 — API-key/shared-secret, NOT a Supabase session).

Purpose: These are the routes CONCERNS flagged HIGH (guest-reachable paid Gemini, no per-request authz) and the one route with NO auth at all (intake). After this plan, every `api/*` route has an enforcement wrapper — satisfying the structural coverage test in Wave 3.
Output: Five route handlers wrapped/guarded + per-route 401/403 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates NEW per-route test files under `src/app/api/ai/**/__tests__/auth.test.ts`. It CONSUMES (does not create) `withAuth` and `consumeGuestRateLimit` from plan 01. If a machine-auth wrapper `withApiKey` is introduced, it is a NEW symbol (in `src/lib/api/with-auth.ts` or a sibling) — record it in the SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap api/ai/* routes in withAuth + rate-limit guest paths (D-04/D-05)</name>
  <read_first>
    - src/lib/api/with-auth.ts (the wrapper from plan 01 — signature + role/guest semantics)
    - src/lib/api/rate-limit.ts (consumeGuestRateLimit from plan 01)
    - src/app/api/ai/people/route.ts (current unguarded POST — analog target shape in 02-PATTERNS.md lines 95-120)
    - src/app/api/ai/section/route.ts (guest-allowed)
    - src/app/api/ai/metric-capture/route.ts (guest-allowed; Zod kind validation, getGeminiRuntimeConfig call)
    - src/app/api/ai/speedtest/route.ts
    - src/app/api/team-logo/route.ts (the already-guarded reference shape)
    - src/lib/supabase/middleware.ts (ALLOW_GUEST_MI_JORNADA whitelist ~68-72 — which routes are guest)
    - src/test/fixtures/user-context.ts (makeUserContext)
    - .planning/codebase/CONCERNS.md (HIGH unguarded AI routes, lines 45-49)
  </read_first>
  <behavior>
    - ai/people POST: no session → 401; role "collaborator" → 403; role "editor"/"admin"/"coordinator" → existing handler runs (200/expected).
    - ai/speedtest: no session → 401 (non-guest, authenticated only).
    - ai/metric-capture POST (guest-allowed): no session → handler runs (not 401); after N guest calls within window → 429 (rate limited).
    - ai/section POST (guest-allowed): same guest + rate-limit behavior.
  </behavior>
  <action>
    Convert each route's `export async function POST(request)` to `export const POST = withAuth(options, async (request, ctx) => { ...existing body... })`.
    - `ai/people`: `withAuth({ roles: ["admin", "editor", "coordinator"] }, ...)` (no guest).
    - `ai/speedtest`: `withAuth({}, ...)` (any authenticated user; confirm against current usage — it is NOT in the guest whitelist).
    - `ai/metric-capture` and `ai/section`: `withAuth({ allowGuest: true }, ...)` per the `ALLOW_GUEST_MI_JORNADA` whitelist. On the guest path (ctx.userId null) call `consumeGuestRateLimit` keyed by client IP (from request headers, e.g. `x-forwarded-for`) and return `NextResponse.json({ error: "Demasiadas solicitudes, intenta más tarde." }, { status: 429 })` when blocked.
    Keep all existing bodies unchanged: Zod `safeParse`→400, `getGeminiRuntimeConfig()` call, MIME/type checks, and bracketed `console.error("[ai][...]")` logging. Do not change the Gemini fetch beyond what already exists.
    Create `__tests__/auth.test.ts` beside each of the four routes covering the behaviors above. Mock `@/lib/auth` getUserContext via the fixture; for guest rate-limit, mock/exercise `consumeGuestRateLimit`. Call the exported `POST` directly with a constructed `new Request(...)`. Mock the Gemini network/`getGeminiRuntimeConfig` so tests don't hit the paid API.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/ai</automated>
  </verify>
  <acceptance_criteria>
    - All four `src/app/api/ai/*/route.ts` files contain `withAuth(`.
    - `metric-capture` and `section` reference the rate limiter on the guest path.
    - `npx vitest run src/app/api/ai` exits 0: per-route 401 (non-guest), 403 (under-priv on people), guest-pass + 429 (rate-limited) for guest routes.
    - No test invokes the real Gemini API.
  </acceptance_criteria>
  <done>Every AI route enforces auth via withAuth; guest routes are rate-limited; HIGH CONCERNS finding closed with tests.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide matches/intake machine-auth model (Open Q1)</name>
  <read_first>
    - src/app/api/matches/intake/route.ts (NO auth today; defensive firstString/getPathValue walkers; external machine caller)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pitfall 2 lines 408-411; Open Q1 lines 462-464)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md (intake machine-route note lines 124-128)
  </read_first>
  <decision>How does the upstream caller of `matches/intake` authenticate, and therefore what wrapper does the route get?</decision>
  <context>`matches/intake` is a machine route with NO session — applying session-based `withAuth({roles})` blindly will break the upstream integration (Pitfall 2). It must still get SOME wrapper so the D-07 coverage test passes. The choice determines whether a `withApiKey` variant is built.</context>
  <options>
    <option id="api-key-header">
      <name>Shared-secret / API-key header</name>
      <pros>Standard machine auth; fail-closed; testable; satisfies coverage test with a real guard.</pros>
      <cons>Requires the upstream caller to send a header and a new env var (e.g. INTAKE_API_KEY).</cons>
    </option>
    <option id="no-caller-auth">
      <name>Caller cannot send any credential</name>
      <pros>No upstream change.</pros>
      <cons>Route stays effectively open; only option is IP allowlist or accept-and-document. Weakest.</cons>
    </option>
  </options>
  <resume-signal>Select: api-key-header (provide env var name) or describe the caller's actual auth capability</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard matches/intake with the chosen machine-auth wrapper</name>
  <read_first>
    - src/app/api/matches/intake/route.ts (the route to guard; preserve all normalization)
    - src/lib/api/with-auth.ts (extend with an apiKey/secret mode, or add a sibling withApiKey)
    - src/lib/env.ts (appEnv accessor pattern — add the intake secret env var here)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (A4 lines 456; machine-route mitigation)
  </read_first>
  <behavior>
    - intake POST with a missing/wrong API key header → 401 (or 403), handler NOT executed.
    - intake POST with the correct header → existing normalization + matches write runs.
  </behavior>
  <action>
    Implement Task 2's decision. If `api-key-header`: add `withApiKey(handler)` (in `src/lib/api/with-auth.ts` or `src/lib/api/with-api-key.ts`) that reads a request header (e.g. `x-intake-key`) and compares to `appEnv.intakeApiKey` (add this to `src/lib/env.ts` reading `process.env.INTAKE_API_KEY` with the existing accessor pattern); mismatch/absent → 401 JSON, else call handler. Wrap intake's `POST` export with it. The matches write inside intake MUST also stamp the actor — coordinate with plan 04 (intake is in the write-path inventory): if no user context exists, stamp `created_by`/`updated_by` to a system/admin profile id or NULL per the import-provenance decision (flag in SUMMARY for plan 04 to reconcile).
    Keep all existing `firstString`/`getPathValue`/`normalizeDate` defensive walkers unchanged.
    Add `src/app/api/matches/intake/__tests__/auth.test.ts`: wrong/missing key → 401; correct key → handler proceeds (mock the supabase write).
    Set `INTAKE_API_KEY` is operator-provided — declare it in the SUMMARY user_setup notes (gitignored `.env.local`).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/matches/intake</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/matches/intake/route.ts` references a machine-auth wrapper (`withApiKey`/`withAuth`).
    - Wrong/missing key returns 401/403 and does not execute the write.
    - Correct key path proceeds.
    - `npx vitest run src/app/api/matches/intake` exits 0.
  </acceptance_criteria>
  <done>The no-auth-at-all intake route now fails closed on machine auth; a wrapper marker is present for the coverage test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest/unauthenticated → api/ai/* | Guest whitelist exposes paid Gemini; rate limit caps abuse. |
| external machine → api/matches/intake | No session; shared-secret header is the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Elevation of Privilege | `api/ai/*` (unguarded today) | mitigate | Task 1 wraps every AI route in `withAuth` (D-04); per-route 401/403 tests. |
| T-02-03 | Denial of Service / financial | guest-reachable paid Gemini (`metric-capture`, `section`) | mitigate | Task 1 `consumeGuestRateLimit` on guest path (D-05); 429 test. |
| T-02-04 | Spoofing / Tampering | `api/matches/intake` (no auth) | mitigate | Tasks 2-3 machine-auth (shared-secret header); fail-closed. |
| T-02-05 | Information Disclosure | Gemini key in URL query (CONCERNS MEDIUM) | accept | Out of scope this phase (CONCERNS refactor deferred); note for follow-up. Not authz. |
</threat_model>

<verification>
- `npx vitest run src/app/api/ai` green: 401/403/guest/429 per route.
- `npx vitest run src/app/api/matches/intake` green: machine-auth fail-closed.
- All five route files contain a wrapper marker (`withAuth(`/`withApiKey`).
</verification>

<success_criteria>
- Every `api/ai/*` route wrapped in `withAuth` (D-04); guest routes rate-limited (D-05).
- `matches/intake` fails closed on a machine-auth check (Open Q1 resolved).
- Per-route 401/403 + guest/rate-limit tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-02-SUMMARY.md` when done. Record any new `withApiKey` symbol and the `INTAKE_API_KEY` env requirement.
</output>
