---
phase: 02-rls-removal-guard-coverage-audit
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/data/dashboard.ts
  - src/lib/data/collaborators.ts
  - src/lib/data/announcements.ts
  - src/app/(dashboard)/people/page.tsx
  - src/lib/data/platform-access.ts
  - src/lib/__tests__/loaders-ctx.test.ts
autonomous: true
requirements: [AUTHZ-01, AUTHZ-02]
user_setup: []

must_haves:
  truths:
    - "Every src/lib/data/* loader takes a typed non-optional UserContext argument (D-06)"
    - "Each RSC page/route caller guards at the boundary and passes ctx down"
    - "(dashboard)/people/page.tsx no longer imports createSupabaseAdminClient (D-09)"
    - "Platform-access for a person is derived without the page reaching the service-role client"
  artifacts:
    - path: "src/lib/data/dashboard.ts"
      provides: "Loaders with ctx: UserContext param"
      contains: "ctx: UserContext"
    - path: "src/lib/data/collaborators.ts"
      provides: "Loaders with ctx param (isUuidLike excluded)"
      contains: "ctx: UserContext"
    - path: "src/lib/data/announcements.ts"
      provides: "Loaders with ctx param"
      contains: "ctx: UserContext"
    - path: "src/app/(dashboard)/people/page.tsx"
      provides: "Guarded page, no admin-client import (D-09)"
    - path: "src/lib/data/platform-access.ts"
      provides: "server-only helper re-deriving platform access (Pitfall 4)"
  key_links:
    - from: "src/app/(dashboard)/people/page.tsx"
      to: "src/lib/data/dashboard.ts getPeopleData"
      via: "getPeopleData(user) passing ctx"
      pattern: "getPeopleData\\(user\\)"
    - from: "src/lib/data/dashboard.ts"
      to: "src/lib/auth.ts UserContext"
      via: "import type UserContext"
      pattern: "UserContext"
---

<objective>
Make all `src/lib/data/*` loaders pure with a typed, non-optional `UserContext` argument (D-06), guard at each RSC page / route boundary and pass ctx down, and stop `(dashboard)/people/page.tsx` from importing the service-role admin client (D-09). This is the read-path half of AUTHZ-01 and the service-role-confinement half of AUTHZ-02.

Purpose: Loaders currently self-fetch with no guard signal; the `ctx` arg is the structural contract the D-07 coverage test (Wave 3) detects. The people page is the one render path reaching `admin.ts` — D-09 confines service-role to server-only mutation modules.
Output: Three loader modules with ctx params, the people page guarded + admin-client-free, a server-only platform-access helper, and a loader-signature unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW: `src/lib/data/platform-access.ts` (server-only helper wrapping the `listUsers`/profiles-derived platform-access read). CONSUMES `UserContext` from plan 01. CHANGED loader signatures gain a `ctx` param — downstream drift verification should treat the param addition as expected.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thread typed ctx into every src/lib/data/* loader + update all callers (D-06)</name>
  <read_first>
    - src/lib/auth.ts (exported UserContext from plan 01; requireUserContext at boundary)
    - src/lib/data/dashboard.ts (getPeopleData :529, getGridCalendarData :239, getGridData :77, getMatchDetailData :330, getRolesData :622 — signatures per 02-PATTERNS.md lines 136-151)
    - src/lib/data/collaborators.ts (getCollaboratorDayData :584, getCollaboratorMatchData :654; isUuidLike :134 is a pure helper — MUST stay ctx-free)
    - src/lib/data/announcements.ts (getActiveAnnouncement :37, getLatestAnnouncement :41)
    - src/app/api/grid/calendar/route.ts (existing boundary-guard caller pattern lines 13-20)
    - all RSC pages/routes that call these loaders (grep for each loader name to find callers)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pattern 2 lines 228-251)
  </read_first>
  <action>
    For every EXPORTED async loader in `src/lib/data/dashboard.ts`, `collaborators.ts`, `announcements.ts`, add a leading required parameter `ctx: UserContext` (`import type { UserContext } from "@/lib/auth"`). The loader still creates its own server client internally via `createSupabaseServerClient()` — D-06 "purity" means not coupling the AUTHORIZATION decision to cookies; the required `ctx` arg is the contract. Do NOT add a ctx param to the pure non-async helper `isUuidLike` (collaborators.ts:134) — it is on the coverage-test allowlist.
    Then update EVERY caller: each RSC page / route handler that calls these loaders must first resolve context at the boundary (`const user = await requireUserContext();` in pages, or `getUserContext()`→401 in routes) and pass it: `getPeopleData(user)`, `getGridData(user, filters)`, etc. Grep each loader name to find all call sites; leave none un-updated (the build will fail typecheck otherwise — that is the safety net here).
    Preserve all existing loader bodies and return types.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Each exported async loader in the three files has `ctx: UserContext` as its first parameter.
    - `isUuidLike` is unchanged (no ctx param).
    - All callers pass a resolved context; `npm run typecheck` exits 0 (no missing-arg errors).
  </acceptance_criteria>
  <done>Loaders are guard-aware by signature; every caller guards at the boundary and threads ctx.</done>
</task>

<task type="auto">
  <name>Task 2: Remove admin-client read from (dashboard)/people + server-only platform-access helper (D-09)</name>
  <read_first>
    - src/app/(dashboard)/people/page.tsx (admin import :48, usage :211-249; boundary guard :168; admin gate :213)
    - src/lib/supabase/admin.ts (server-only precedent — the helper may still use it)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md (D-09 block lines 161-174; Service-role isolation lines 243-245)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pitfall 4 lines 418-421)
  </read_first>
  <action>
    Remove the `import { createSupabaseAdminClient } from "@/lib/supabase/admin"` from `src/app/(dashboard)/people/page.tsx` (line 48) and the inline `createSupabaseAdminClient()` + `auth.admin.listUsers(...)` usage block (lines 215-249).
    Create `src/lib/data/platform-access.ts` with `import "server-only"` exporting a helper that re-derives whether a person has platform access. Prefer deriving from `profiles` via the normal server client (a person with a matching `profiles` row has access); if the auth.users `listUsers` read is genuinely still required, that read lives ONLY in this server-only helper (which may import `admin.ts`) — never in the page module. The page calls this helper. Keep the existing boundary guard (`requireUserContext()` line 168) and the `user.role === "admin"` gate (line 213).
    Update the `getPeopleData()` call (line 169) to `getPeopleData(user)` per plan-03 Task 1.
    Bracketed `[platform-access]` console prefix on errors.
  </action>
  <verify>
    <automated>! grep -q "createSupabaseAdminClient" "src/app/(dashboard)/people/page.tsx" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/people/page.tsx` contains NO reference to `createSupabaseAdminClient` or `@/lib/supabase/admin`.
    - `src/lib/data/platform-access.ts` exists, starts with `import "server-only"`, and is the only new module that may reach `admin.ts`.
    - `selectedPersonHasPlatformAccess` is still computed (behavior preserved).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The people page no longer reaches the service-role client; service-role stays confined to server-only modules (AUTHZ-02 criterion 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Loader-signature + platform-access unit tests</name>
  <read_first>
    - src/test/fixtures/user-context.ts (makeUserContext from plan 01)
    - src/lib/data/announcements.ts (simplest loaders to unit-test by passing a fake ctx)
    - src/lib/data/platform-access.ts (the new helper)
  </read_first>
  <behavior>
    - A loader can be invoked with a fake `makeUserContext()` ctx (mocking the supabase client) and returns its expected shape — proving the ctx arg is the contract and loaders are unit-testable without cookies.
    - platform-access helper returns access=true for a person with a profiles row, false otherwise (mock the client/admin read).
  </behavior>
  <action>
    Create `src/lib/__tests__/loaders-ctx.test.ts`. Mock `@/lib/supabase/server` `createSupabaseServerClient` to return a stub query builder. Invoke at least one loader (e.g. `getActiveAnnouncement(makeUserContext())`) and assert it runs with the passed ctx and returns the expected shape. Add a test for `platform-access.ts` asserting the access-derivation logic (mock the read). Use `vi.mock` + the `makeUserContext` fixture.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/loaders-ctx.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test passes a fake ctx into a loader without any cookies/Supabase session (proves D-06 testability).
    - platform-access derivation test green.
    - `npx vitest run src/lib/__tests__/loaders-ctx.test.ts` exits 0.
  </acceptance_criteria>
  <done>Pure-loader contract proven testable; platform-access logic covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page/route → data loader | Authorization decided at the boundary; loaders receive ctx, never decide auth from cookies. |
| page module → service-role admin client | D-09 removes this reach; service-role confined to server-only helpers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | data loaders relying on RLS for read authz | mitigate | Task 1 boundary-guard + ctx contract (D-06); read paths now guarded app-side. |
| T-02-07 | Information Disclosure | `(dashboard)/people` service-role read at render | mitigate | Task 2 removes admin-client from the page; confines service-role to a server-only helper (D-09). |
| T-02-08 | Elevation of Privilege | forgotten ctx arg on a future loader | mitigate | Structural coverage test in plan 05 (D-07) enforces the ctx contract. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (all callers thread ctx).
- `src/app/(dashboard)/people/page.tsx` has zero `createSupabaseAdminClient` references.
- `npx vitest run src/lib/__tests__/loaders-ctx.test.ts` green.
</verification>

<success_criteria>
- Every `src/lib/data/*` loader takes a typed non-optional `UserContext` (D-06).
- All callers guard at the boundary and pass ctx down.
- People page no longer imports the admin client; service-role confined to a server-only helper (D-09).
- Loader purity + platform-access logic covered by tests.
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-03-SUMMARY.md` when done. List the final set of loaders changed and the platform-access helper signature.
</output>
