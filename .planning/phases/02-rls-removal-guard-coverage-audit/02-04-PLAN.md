---
phase: 02-rls-removal-guard-coverage-audit
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/audit.ts
  - src/app/actions/matches.ts
  - src/app/actions/people.ts
  - src/app/actions/roles.ts
  - src/app/actions/settings.ts
  - src/app/actions/auth.ts
  - src/app/api/collaborator-reports/route.ts
  - src/app/api/collaborator-reports/__tests__/auth.test.ts
  - src/lib/__tests__/audit.test.ts
  - src/lib/__tests__/stamping-coverage.test.ts
autonomous: true
requirements: [AUTHZ-01, AUTHZ-03]
user_setup: []

must_haves:
  truths:
    - "Every domain insert sets created_by/updated_by from ctx.userId via a shared helper"
    - "Every domain update sets updated_by from ctx.userId"
    - "Every domain mutation writes an audit_log row with changed_by = ctx.userId (never NULL)"
    - "secret_value is redacted from the app_settings audit before/after payload"
    - "collaborator-reports is wrapped in withAuth (carries the D-07 coverage marker) and returns 401 no-session / 403 under-privileged"
    - "A structural test asserts no domain insert/update in src/app/actions/* lacks an accompanying writeAudit call"
  artifacts:
    - path: "src/lib/audit.ts"
      provides: "stampInsert/stampUpdate + writeAudit (app-side actor stamping, AUTHZ-03)"
      exports: ["stampInsert", "stampUpdate", "writeAudit"]
    - path: "src/app/actions/matches.ts"
      provides: "matches/assignments writes stamped + audited"
      contains: "stampInsert"
    - path: "src/app/api/collaborator-reports/route.ts"
      provides: "withAuth-wrapped POST (D-04 marker) + stamped/audited collaborator_reports write"
      contains: "withAuth("
    - path: "src/lib/__tests__/audit.test.ts"
      provides: "changed_by-never-NULL + secret redaction tests"
    - path: "src/app/api/collaborator-reports/__tests__/auth.test.ts"
      provides: "Per-route 401/403 auth test for collaborator-reports (criterion 2)"
    - path: "src/lib/__tests__/stamping-coverage.test.ts"
      provides: "Structural completeness test — every action write site is paired with writeAudit (Pitfall 1)"
      contains: "actions"
  key_links:
    - from: "src/app/actions/matches.ts"
      to: "src/lib/audit.ts"
      via: "stampInsert/stampUpdate + writeAudit on every write"
      pattern: "writeAudit"
    - from: "src/lib/audit.ts"
      to: "audit_log table"
      via: "supabase.from('audit_log').insert with changed_by = ctx.userId"
      pattern: "audit_log"
    - from: "src/app/api/collaborator-reports/route.ts"
      to: "src/lib/api/with-auth.ts"
      via: "import withAuth"
      pattern: "withAuth"
---

<objective>
Move actor stamping out of the `auth.uid()` DB triggers into the app layer (AUTHZ-03): build `src/lib/audit.ts` (`stampInsert`/`stampUpdate`/`writeAudit`) porting the `set_row_metadata` + `log_audit_event` trigger logic, then wire it into EVERY write path so `created_by`/`updated_by` are set and `audit_log.changed_by` is populated (NEVER NULL) on every mutation. Also bring `collaborator-reports` under the `withAuth` convention (D-04) — it is one of the three routes that today guard via inline `getUserContext()`→401 with no structural marker, and this plan already owns that file, so its conversion + per-route auth test land here (AUTHZ-01).

Purpose: This is the highest-risk item in the phase. App code today sets NONE of these — the triggers do it via `auth.uid()`. The moment the Wave 4 teardown drops those triggers, every write loses its actor stamp unless this stamping is already live and verified (D-02). A single missed write site silently yields NULL `changed_by` post-teardown (Pitfall 1), so this plan adds a STRUCTURAL completeness test, not just per-call audits. This plan MUST be complete and green before the teardown migration runs.
Output: The stamping/audit helper, every write path stamping + auditing, collaborator-reports converted to withAuth with a per-route auth test, a structural stamping-coverage test, and the changed_by-never-NULL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW: `stampInsert`, `stampUpdate`, `writeAudit` (`src/lib/audit.ts`). CONSUMES `UserContext` and `withAuth` from plan 01. No new columns are added — `created_by`/`updated_by`/`changed_by` columns already exist (the app now populates them). NOTE: `src/app/api/matches/intake/route.ts` stamping is owned by plan 02 (which owns that file) — this plan does NOT modify intake; reconcile via the SUMMARY. This plan owns `collaborator-reports/route.ts` and converts it to `withAuth` (the D-07 marker for that route comes from here, not plan 02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build src/lib/audit.ts — port set_row_metadata + log_audit_event to app (AUTHZ-03)</name>
  <read_first>
    - supabase/migrations/0001_initial.sql (set_row_metadata lines 90-104; log_audit_event lines 106-162; audit_log schema lines 72-82 — the exact logic + columns to port, per 02-PATTERNS.md lines 65-92)
    - src/app/api/collaborator-reports/route.ts (line 124 reporter_profile_id stamp — the only existing app-side actor-stamp precedent)
    - src/lib/auth.ts (UserContext from plan 01 — ctx.userId is the actor)
    - src/test/fixtures/user-context.ts (makeUserContext)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pattern 3 lines 253-277; Open Q4 audit granularity lines 469-470)
  </read_first>
  <behavior>
    - stampInsert(ctx, payload): returns payload merged with created_by=ctx.userId, updated_by=ctx.userId, created_at/updated_at timestamps (coalesce existing created_at like the trigger).
    - stampUpdate(ctx, payload): returns payload merged with updated_by=ctx.userId, updated_at=now.
    - writeAudit(supabase, ctx, {table, recordId, matchId, action, before, after}): inserts an audit_log row with changed_by=ctx.userId — assert changed_by is non-NULL.
    - writeAudit on the app_settings table path: secret_value is redacted (absent/masked) in before/after.
  </behavior>
  <action>
    Create `src/lib/audit.ts` (`import type { UserContext } from "@/lib/auth"`, bracketed `[audit]` console prefix). Export:
    - `stampInsert<T>(ctx, payload)`: merge `created_by: ctx.userId, updated_by: ctx.userId, created_at: coalesce(payload.created_at, nowIso), updated_at: nowIso` (mirror set_row_metadata lines 90-104).
    - `stampUpdate<T>(ctx, payload)`: merge `updated_by: ctx.userId, updated_at: nowIso`.
    - `writeAudit(supabase, ctx, args)`: insert into `audit_log` columns `table_name, record_id, match_id, action, changed_by, before, after`. Apply the trigger's match_id rule (log_audit_event lines 122-136): table `matches` → record id; `assignments` → row's `match_id`; else null. `changed_by = ctx.userId`. Capture before/after jsonb for parity (Open Q4). When `args.table === "app_settings"`, redact `secret_value` from before/after (CONCERNS LOW finding). On insert error, `console.error("[audit] ...")` and rethrow (audit failure must not be silent).
    Create `src/lib/__tests__/audit.test.ts`: assert stampInsert/stampUpdate set the actor fields; assert writeAudit inserts with non-NULL changed_by (mock supabase, capture the insert payload); assert app_settings audit redacts secret_value; assert match_id derivation for matches/assignments/other.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/audit.ts` exports `stampInsert`, `stampUpdate`, `writeAudit`.
    - writeAudit always sets `changed_by = ctx.userId` (test asserts non-NULL).
    - app_settings audit path redacts `secret_value`.
    - `npx vitest run src/lib/__tests__/audit.test.ts` exits 0.
  </acceptance_criteria>
  <done>App-side stamping + audit writer exists, ported from the triggers, with the never-NULL guarantee proven.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stamping + writeAudit into every action write path + structural completeness test</name>
  <read_first>
    - src/lib/audit.ts (from Task 1)
    - src/app/actions/matches.ts (matches insert createMatchAction ~210-228; matches update; assignments upsert lines ~152,180,257,373,429,487 — NONE currently set created_by, per 02-PATTERNS.md line 237)
    - src/app/actions/people.ts (people insert/update ~145; service-role auth-user creation stays)
    - src/app/actions/roles.ts (role upserts/deletes)
    - src/app/actions/settings.ts (app_settings upsert ~99-132)
    - src/app/actions/auth.ts (profile-related writes)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (write-path inventory lines 389-398; Pitfall 1 lines 402-406)
  </read_first>
  <action>
    For EVERY insert/update in `src/app/actions/{matches,people,roles,settings,auth}.ts`, the action already resolves context via `requireEditor()`/`requireUserContext()` — use that returned ctx. Wrap each insert payload in `stampInsert(ctx, payload)` and each update payload in `stampUpdate(ctx, payload)`. After each successful mutation, call `writeAudit(supabase, ctx, { table, recordId, matchId, action, before, after })`. For updates, fetch the prior row (or use the existing fetched row) as `before` where the old trigger captured it. Cover ALL call sites in the inventory — a missed write path is the Pitfall 1 failure mode (changed_by NULL after teardown).
    Do NOT change the service-role auth-user creation in people.ts (legitimate, stays). Preserve existing redirect/`rethrowNavigationError` error handling (actions throw/redirect, not JSON).
    For `settings.ts` app_settings writes, the writeAudit call benefits from Task 1's secret_value redaction automatically.
    Then create `src/lib/__tests__/stamping-coverage.test.ts` — a STRUCTURAL completeness test (mirroring the D-07 route-coverage approach): statically read every `src/app/actions/*.ts`, find each domain-table mutation call (`.from("<table>").insert(` / `.update(` / `.upsert(`), and assert each is accompanied by a `stampInsert`/`stampUpdate` wrap AND that the file containing it references `writeAudit`. Maintain an EXPLICIT allowlist constant for legitimate non-domain mutations that must NOT be audited as domain writes (e.g. the service-role `auth.admin.createUser` path in people.ts, and any `audit_log` self-insert) — documented inline so exceptions are visible in diffs. Include a predicate self-check against an inline sample (a stamped vs unstamped mutation string) so the test cannot silently pass by matching nothing. Assert the offenders array is empty. This prevents a silently-missed write site from yielding NULL `changed_by` post-teardown (Pitfall 1).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/stamping-coverage.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/actions/matches.ts` insert payloads pass through `stampInsert` (createMatchAction now sets created_by/updated_by).
    - Every write path in the RESEARCH inventory calls `stampInsert`/`stampUpdate` AND `writeAudit`.
    - `src/lib/__tests__/stamping-coverage.test.ts` enumerates every action-file domain mutation and asserts each is paired with stamping + an in-file `writeAudit`; offenders array is empty; the predicate self-check passes; the allowlist (service-role createUser, audit_log self-insert) is explicit.
    - `npm run typecheck` exits 0.
    - No action write path remains that mutates a domain table without an audit call.
  </acceptance_criteria>
  <done>Every server-action write stamps the actor and writes an audit row app-side; a structural test fails CI if any future/missed write site skips stamping (Pitfall 1 closed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert collaborator-reports to withAuth + stamp/audit + per-route auth test</name>
  <read_first>
    - src/lib/audit.ts (from Task 1)
    - src/app/api/collaborator-reports/route.ts (POST: inline `getUserContext()`→401 :88-95, then per-match access check → 403 :103-113, then collaborator_reports upsert; already stamps reporter_profile_id :123 — still relies on trigger for created_by/audit)
    - src/lib/api/with-auth.ts (the wrapper from plan 01 — `withAuth({}, handler)` produces 401 no-session; the route keeps its own per-match 403 inside the body)
    - src/test/fixtures/user-context.ts
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (write-path inventory lines 389-398)
  </read_first>
  <behavior>
    - collaborator-reports POST: no session → 401 (now produced by withAuth, not the inline block).
    - collaborator-reports POST: authenticated but without access to the requested match/assignment → 403 (existing per-match check preserved inside the handler).
    - A collaborator_reports upsert through the route produces a stamped payload (created_by/updated_by set) AND an audit_log row with non-NULL changed_by.
  </behavior>
  <action>
    Convert `src/app/api/collaborator-reports/route.ts` to `export const POST = withAuth({}, async (request, ctx) => { ...existing body... })`. This is the third inline-guarded route (alongside team-logo + grid/calendar in plan 02) — it must carry the `withAuth(` structural marker so plan 05's D-07 coverage test passes. Remove the inline `getUserContext()` call and its `if (!user.userId)` 401 block (lines ~88-95) — `withAuth({})` now produces the 401. KEEP the body's later per-match access check that returns 403 (`access.trialAccess || !assignmentsForMatch.some(...)`) — that is finer-grained authz withAuth does not replace; use `ctx` in place of the removed `user`. Keep the Zod parse→400 and `isUuidLike` demo-mode 400 unchanged.
    Then pass `ctx` through `stampInsert`/`stampUpdate` on the collaborator_reports payload and call `writeAudit(supabase, ctx, { table: "collaborator_reports", recordId, matchId, action, before, after })` after the upsert. Keep the existing `reporter_profile_id: ctx.userId` field. Bracketed prefix preserved.
    Create `src/app/api/collaborator-reports/__tests__/auth.test.ts` (criterion 2 per-route auth test): no session → 401; authenticated-but-under-privileged (no access to the match) → 403; mock `@/lib/auth` getUserContext via the fixture and the collaborator-match-access data + supabase upsert. Add (in `src/lib/__tests__/audit.test.ts` or the route test) an assertion that the produced audit payload has a non-NULL `changed_by`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/audit.test.ts src/app/api/collaborator-reports && grep -q 'withAuth(' src/app/api/collaborator-reports/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/collaborator-reports/route.ts` contains `withAuth(` and no longer has a bare inline `getUserContext()`→401 block; the per-match 403 check is preserved.
    - The route calls `stampInsert`/`stampUpdate` + `writeAudit`; a write produces a non-NULL `changed_by` (asserted).
    - `src/app/api/collaborator-reports/__tests__/auth.test.ts` asserts 401 (no session) and 403 (under-privileged).
    - `npx vitest run src/lib/__tests__/audit.test.ts src/app/api/collaborator-reports` exits 0.
  </acceptance_criteria>
  <done>collaborator-reports carries the withAuth marker (D-07 ready), stamps + audits app-side with a per-route 401/403 test, and proves the never-NULL guarantee for a real write path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app write path → audit_log | Actor attribution now app-set (ctx.userId), not auth.uid(); must never produce NULL changed_by. |
| audit payload → audit_log storage | Secret columns must not be persisted in plaintext (CONCERNS LOW). |
| authenticated session → api/collaborator-reports | Session route; withAuth is the (now structural) 401 seam; per-match 403 stays in the handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Repudiation | actor attribution lost after triggers drop | mitigate | Tasks 1-3 app-side stamping + writeAudit on every write, verified BEFORE Wave 4 teardown (D-02/AUTHZ-03); changed_by-never-NULL test. |
| T-02-10 | Information Disclosure | secret_value persisted in audit_log | mitigate | Task 1 redacts secret_value from app_settings audit before/after (CONCERNS LOW). |
| T-02-11 | Repudiation | a forgotten write path skips stamping | mitigate | Task 2 covers full RESEARCH inventory AND adds a structural stamping-coverage test that fails CI on any unstamped action mutation (Pitfall 1); reconcile intake (plan 02) in SUMMARY. |
| T-02-17 | Elevation of Privilege | collaborator-reports inline-guarded (marker-invisible to D-07) | mitigate | Task 3 converts to `withAuth({})`; per-match 403 preserved; per-route 401/403 test added. |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/audit.test.ts` green: stamping sets actor, changed_by never NULL, secret_value redacted, match_id rule correct.
- `npx vitest run src/lib/__tests__/stamping-coverage.test.ts` green: every action mutation paired with stamping + writeAudit; offenders empty.
- `npx vitest run src/app/api/collaborator-reports` green: 401 no-session, 403 under-privileged, non-NULL changed_by.
- `npm run typecheck` exits 0 after wiring all action write paths.
</verification>

<success_criteria>
- `stampInsert`/`stampUpdate`/`writeAudit` exist and port the trigger logic (AUTHZ-03).
- Every action + collaborator-reports write stamps the actor and writes an audit row.
- `audit_log.changed_by` is provably never NULL on app writes.
- A structural stamping-coverage test fails CI if any action write site skips stamping (Pitfall 1 closed).
- collaborator-reports converted to withAuth with a per-route 401/403 test (AUTHZ-01 / criterion 2).
- secret_value redacted from audit.
- Intake stamping reconciled with plan 02 (noted in SUMMARY).
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-04-SUMMARY.md` when done. Confirm the full write-path inventory is covered, that collaborator-reports now carries the withAuth marker (so plan 05's coverage allowlist stays at just `api/health`), and note the intake reconciliation with plan 02.
</output>
</content>
</invoke>
