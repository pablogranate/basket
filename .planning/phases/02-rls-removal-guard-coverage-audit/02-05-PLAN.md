---
phase: 02-rls-removal-guard-coverage-audit
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/lib/api/__tests__/guard-coverage.test.ts
autonomous: false
requirements: [AUTHZ-01]
user_setup: []

must_haves:
  truths:
    - "A structural test asserts every api/*/route.ts is wrapped in a guard marker (D-07)"
    - "A structural test asserts every src/lib/data/* exported async loader takes a ctx arg (D-07)"
    - "All 9 api/* routes (4 ai/*, intake, team-logo, grid/calendar, collaborator-reports) carry a wrapper marker; only api/health is allowlisted"
    - "The coverage test FAILS if a future route/loader forgets its guard"
    - "The full suite + npm run check are green before the destructive migration runs"
  artifacts:
    - path: "src/lib/api/__tests__/guard-coverage.test.ts"
      provides: "Structural guard-coverage test (D-07)"
      contains: "globSync"
  key_links:
    - from: "src/lib/api/__tests__/guard-coverage.test.ts"
      to: "src/app/api/**/route.ts + src/lib/data/*.ts"
      via: "static file read + marker assertion"
      pattern: "route\\.ts|lib/data"
---

<objective>
Build the CI-enforced structural guard-coverage test (D-07): a static-analysis test that enumerates every `api/*/route.ts` and every `src/lib/data/*` loader and asserts a guard marker is present — a forgotten guard fails CI rather than shipping open. Then run the full verification gate (suite + `npm run check` + manual end-to-end) confirming guards + stamping are proven BEFORE the destructive teardown migration (D-02 step 3).

Purpose: This is the structural safety net the user specifically wanted (a first-class deliverable, not nice-to-have). It validates that plans 02-04 closed the entire surface — all 9 `api/*` routes now carry a `withAuth(`/`withApiKey(` marker (4 ai/* + intake from plan 02; team-logo + grid/calendar from plan 02; collaborator-reports from plan 04), with only `api/health` allowlisted. This plan is the D-02 "verify guards (tests + manual)" gate that must pass before Wave 4.
Output: The coverage test + a green full suite + a manual verification sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-02-SUMMARY.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-03-SUMMARY.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW: `src/lib/api/__tests__/guard-coverage.test.ts` (D-07 structural test). No production-code symbols. CONSUMES the wrapped routes (plan 02 — ai/*, intake, team-logo, grid/calendar), the collaborator-reports wrapper (plan 04), the ctx'd loaders (plan 03), and stamping (plan 04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structural guard-coverage test (D-07)</name>
  <read_first>
    - src/app/api/**/route.ts (ALL 9 routes — every one must now contain a wrapper marker: 4 ai/* + intake + team-logo + grid/calendar from plan 02, collaborator-reports from plan 04; only health is unguarded)
    - src/lib/data/*.ts (all loaders — must take ctx after plan 03; isUuidLike is the allowlisted exception)
    - src/app/api/health/route.ts (the ONLY explicit allowlist exception — health is intentionally unguarded)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pattern 4 lines 279-307; recommendation: static source analysis with explicit allowlist)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md (guard-coverage.test.ts note lines 208; allowlist isUuidLike + api/health)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-02-SUMMARY.md (confirm intake's wrapper marker name, e.g. withApiKey; confirm team-logo/grid/calendar converted)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-04-SUMMARY.md (confirm collaborator-reports carries the withAuth marker)
  </read_first>
  <behavior>
    - Test "every api route is guarded": globs `src/app/api/**/route.ts`, reads each, asserts it contains a wrapper marker (`withAuth(` OR `withApiKey(` per plan 02's intake decision); offenders array (excluding `api/health/route.ts`) is empty. All 9 routes pass: ai/people, ai/section, ai/metric-capture, ai/speedtest, matches/intake, team-logo, grid/calendar, collaborator-reports (+ health allowlisted).
    - Test "every data loader requires a ctx arg": globs `src/lib/data/*.ts`, asserts each exported async function declares a `ctx`/`context` first param; `isUuidLike` is allowlisted.
    - Negative self-check: a deliberately-unguarded temp fixture string would be flagged (assert the offender-detection logic actually catches a missing marker — e.g. by testing the predicate against a sample source string).
  </behavior>
  <action>
    Create `src/lib/api/__tests__/guard-coverage.test.ts`. Use `globSync` (Node `node:fs` globSync on Node 22+, or `fast-glob` if needed — confirm Node version; fall back to a manual recursive readdir if globSync unavailable).
    - Route coverage: read each `src/app/api/**/route.ts`; an offender is a file NOT containing `withAuth(` AND NOT containing the intake machine-auth marker (`withApiKey(` or whatever plan 02 chose). Maintain an EXPLICIT allowlist constant: `["src/app/api/health/route.ts"]` — and ONLY health (all three formerly-inline-guarded routes — team-logo, grid/calendar, collaborator-reports — are now wrapped, so they must NOT be allowlisted). Assert `offenders` equals `[]`.
    - Loader coverage: read each `src/lib/data/*.ts`; for each `export async function NAME(` assert the first param matches `/^(ctx|context)\b/`. Maintain an explicit allowlist for intentional ctx-free helpers: `isUuidLike`. Assert no offenders.
    - Include a unit assertion of the offender predicate against an inline sample (guarded vs unguarded string) so the test can't silently pass by matching nothing.
    Bracketed comments explaining the allowlist (only `api/health`) so exceptions are visible in diffs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/__tests__/guard-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/api/__tests__/guard-coverage.test.ts` enumerates routes + loaders via static read.
    - All 9 current routes pass (every `api/*` wrapped with `withAuth(`/`withApiKey(`; only `api/health` allowlisted).
    - All current loaders pass (ctx arg; only `isUuidLike` allowlisted).
    - The predicate self-check proves the detector catches a missing marker.
    - `npx vitest run src/lib/api/__tests__/guard-coverage.test.ts` exits 0.
  </acceptance_criteria>
  <done>A forgotten guard on any future route/loader fails CI; the entire current surface (all 9 routes, only health allowlisted) is structurally confirmed guarded (D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + check gate (D-02 step 3 — verify BEFORE teardown)</name>
  <read_first>
    - package.json (test + check scripts from plan 01)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Sampling Rate lines 512-515; phase gate)
  </read_first>
  <action>
    Run `npm run test` (full Vitest suite) and `npm run check` (lint + typecheck + test + build). Both MUST be green. This is the D-02 step-3 gate: guards (plans 02-03) + stamping (plan 04) + coverage (Task 1) are all proven WHILE Supabase Auth + RLS are still live — before the Wave 4 destructive migration. If anything is red, STOP — do not proceed to the teardown plan.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 (all suites: with-auth, settings-secret, ai routes, team-logo, grid/calendar, intake, loaders-ctx, audit, stamping-coverage, collaborator-reports, guard-coverage).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>The full automated gate is green with RLS + stamping triggers still live — the safe state to verify against per D-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end guard verification (RSC pages — Vitest cannot test async RSC)</name>
  <read_first>
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pitfall 5 lines 423-426 — async RSC pages untestable in Vitest; cover via manual)
  </read_first>
  <what-built>Plans 02-04 + Task 1 added withAuth to every route (all 9, only health unguarded), ctx to every loader, stamping to every write, and a structural coverage test — all with RLS STILL LIVE. Async RSC pages (e.g. `(dashboard)/people`) cannot be unit-tested in Vitest, so they need manual confirmation.</what-built>
  <how-to-verify>
    1. `npm run dev`. Log in as an admin; visit `/people` — confirm the page renders, the platform-access toggle still shows correctly (D-09 change didn't break it), and people list loads.
    2. Log in (or simulate) as a non-admin/collaborator; confirm role-gated sections behave as before. Confirm the guest mi-jornada AI feature still works (Gemini key reaches the runtime read — D-05 not regressed by the D-08 redaction change).
    3. Hit a guarded AI route with no session (e.g. `curl -X POST <host>/api/ai/people`) → expect 401. With an under-privileged session → expect 403.
    4. Hit a guest AI route (`/api/ai/metric-capture`) repeatedly → expect a 429 after the limit.
    5. Hit `matches/intake` without the API key → expect 401/403; with the key → accepted.
    6. Hit `team-logo` and `grid/calendar` with no session → expect 401; with a session → expect data (confirm the withAuth conversion didn't break them).
    7. Hit `collaborator-reports` with no session → 401; authenticated but without access to the match → 403.
    8. Perform one real domain write (create a match) and confirm in the DB that `audit_log.changed_by` is populated (non-NULL) and `created_by`/`updated_by` are set — proving app-side stamping works WHILE triggers still exist (they should agree).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms: pages render + role gating intact; guest AI still works; route 401/403; guest 429; intake machine-auth; team-logo/grid/calendar 401-then-pass; collaborator-reports 401/403; a real write shows non-NULL changed_by/created_by.
    - If any check fails, the teardown plan (06) is BLOCKED until fixed.
  </acceptance_criteria>
  <resume-signal>Type "verified" (guards + stamping confirmed live) or describe failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code → guard surface | New routes/loaders must be guarded; the structural test is the CI boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | a forgotten guard on a new route/loader | mitigate | Task 1 structural coverage test (D-07) fails CI on any unguarded route/loader; allowlist is only `api/health`. |
| T-02-13 | Repudiation / EoP | guards/stamping not actually working before teardown | mitigate | Tasks 2-3 full-suite + manual gate (D-02 step 3) confirm everything works WHILE RLS is still live. |
</threat_model>

<verification>
- `npx vitest run src/lib/api/__tests__/guard-coverage.test.ts` green; offender lists empty (all 9 routes wrapped, only health allowlisted).
- `npm run check` exits 0.
- Operator sign-off on manual RSC + route + stamping verification.
</verification>

<success_criteria>
- Structural guard-coverage test exists and passes (D-07); forgotten guards fail CI; only `api/health` allowlisted.
- Full suite + `npm run check` green WHILE RLS + triggers are still live.
- Manual end-to-end verification signed off — guards + app-side stamping proven (D-02 step 3 satisfied).
- Wave 4 teardown is now unblocked.
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-05-SUMMARY.md` when done. Explicitly state the D-02 step-3 gate is satisfied and the teardown migration is cleared to proceed.
</output>
</content>
</invoke>
