---
phase: 02-rls-removal-guard-coverage-audit
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql
autonomous: false
requirements: [AUTHZ-02]
user_setup: []

must_haves:
  truths:
    - "All RLS policies are dropped and RLS disabled on every domain table (D-01)"
    - "All auth.uid()-based triggers (set_row_metadata, log_audit_event) are dropped (D-03)"
    - "The RLS helper functions can_read/can_edit/current_app_role are dropped"
    - "After the migration is pushed, a real write still records non-NULL audit_log.changed_by (app-side stamping holds without triggers)"
    - "handle_new_user/on_auth_user_created and created_by/updated_by/changed_by columns are NOT dropped"
  artifacts:
    - path: "supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql"
      provides: "Teardown migration: drop policies + auth.uid() triggers + helper fns (AUTHZ-02, D-01/D-02/D-03)"
      contains: "disable row level security"
  key_links:
    - from: "supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql"
      to: "live Supabase DB"
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
Land the destructive teardown migration LAST (D-02): drop every RLS policy + disable RLS on all domain tables (D-01), drop the `auth.uid()`-based `set_row_metadata`/`log_audit_event` triggers and their helper functions (D-03), then push it to the live DB and verify app-side stamping still works without the triggers. This satisfies AUTHZ-02 (Supabase becomes plain Postgres, RLS no longer the backstop).

Purpose: This is the ONLY irreversible step in the phase and it runs ONLY after plan 05 proved guards + stamping work while RLS was still live. The app must never have both RLS and app-side stamping absent — this plan removes RLS only because plan 04 already moved stamping into the app and plan 05 verified it.
Output: The `0010` migration file, applied to the live DB via `supabase db push`, with a post-push write confirming non-NULL `changed_by`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-CONTEXT.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-PATTERNS.md
@.planning/phases/02-rls-removal-guard-coverage-audit/02-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW: `supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql`. This DROPS database objects (policies, triggers, helper functions) — it does NOT create app symbols. It explicitly does NOT drop the `created_by`/`updated_by`/`changed_by` columns, the `app_role` enum, or `handle_new_user`/`on_auth_user_created` (Phase 4).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm plan 05 gate satisfied + teardown clearance (D-02)</name>
  <read_first>
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-05-SUMMARY.md (must state the D-02 step-3 gate is satisfied)
  </read_first>
  <decision>Are guards (plans 02-03) + app-side stamping (plan 04) verified green WHILE RLS is still live, clearing the destructive migration to proceed?</decision>
  <context>D-02 is non-negotiable: the teardown lands LAST, only after guards + stamping are proven. The app must never have both RLS and app-side stamping absent. This checkpoint is the explicit gate before the only irreversible step.</context>
  <options>
    <option id="proceed">
      <name>Proceed — gate satisfied</name>
      <pros>Plan 05 confirmed full suite + manual verification green with RLS live.</pros>
      <cons>None — this is the planned path.</cons>
    </option>
    <option id="block">
      <name>Block — a verification failed</name>
      <pros>Prevents removing RLS while stamping/guards are unproven.</pros>
      <cons>Requires fixing plan 02-05 gaps first (gap-closure).</cons>
    </option>
  </options>
  <resume-signal>Select: proceed (gate satisfied) or block (describe the unmet verification)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the 0010 teardown migration (D-01/D-03)</name>
  <read_first>
    - supabase/migrations/0009_add_contacts.sql (file style: lowercase keywords, -- header, drop if exists, public.-qualified — analog per 02-PATTERNS.md lines 178-197)
    - supabase/migrations/0001_initial.sql (policies + set_row_metadata + log_audit_event + can_read/can_edit/current_app_role definitions)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (RLS + Trigger Teardown Surface lines 341-373 — the EXACT drop inventory + the Do-NOT-drop list)
  </read_first>
  <action>
    Create `supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql` with a leading `-- 0010: Drop RLS policies + auth.uid() triggers (Phase 2 / D-01 / D-02 / D-03).` comment. Following the 0009 style (lowercase, `public.`-qualified, `if exists` guards), drop EXACTLY the inventory in RESEARCH lines 341-373:
    - For each table (profiles, people, roles, matches, assignments, audit_log, announcements, collaborator_reports, app_settings, club_contacts): `drop policy if exists "<name>" on public.<table>;` for every policy listed, then `alter table public.<table> disable row level security;`.
    - Drop every `set_row_metadata` trigger (people_metadata, roles_metadata, matches_metadata, assignments_metadata, announcements_metadata, collaborator_reports_metadata, app_settings_metadata, club_contacts_metadata) and every `log_audit_event` trigger (people_audit, roles_audit, matches_audit, assignments_audit, announcements_audit, collaborator_reports_audit, app_settings_audit, club_contacts_audit) with `drop trigger if exists <name> on public.<table>;`.
    - After triggers/policies are gone, `drop function if exists public.set_row_metadata();`, `drop function if exists public.log_audit_event();`, `drop function if exists public.can_edit();`, `drop function if exists public.can_read();`, `drop function if exists public.current_app_role();`.
    DO NOT drop (RESEARCH lines 368-372): `handle_new_user()` + `on_auth_user_created` (Phase 4), the `created_by`/`updated_by`/`changed_by` columns, the `app_role` enum. Order: triggers → policies → disable RLS → functions (functions last, after their referencing objects are gone).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql && grep -qi "disable row level security" supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql && ! grep -qi "drop function if exists public.handle_new_user" supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists; contains `disable row level security` for all 10 tables.
    - Drops all set_row_metadata + log_audit_event triggers and the 5 helper functions.
    - Does NOT drop handle_new_user, created_by/updated_by/changed_by columns, or app_role enum.
    - Drop order is triggers/policies before functions.
  </acceptance_criteria>
  <done>The teardown migration is authored against the verified RESEARCH inventory, ready to push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply the teardown migration to the live DB (supabase db push)</name>
  <read_first>
    - supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql (the migration to apply)
    - CLAUDE.md (migrations applied manually; Supabase cloud)
  </read_first>
  <what-built>The 0010 teardown migration file (Task 2). It is NOT yet applied — build/typecheck pass without it because types come from config, not the live DB (false-positive risk). This task applies it. This is the LAST destructive step per D-02 and is irreversible.</what-built>
  <how-to-verify>
    1. Ensure plan 05's manual gate was signed off (guards + stamping verified live).
    2. Apply the migration: run `supabase db push` (set `SUPABASE_ACCESS_TOKEN` for non-TTY). If `supabase db push` prompts interactively and cannot be suppressed, apply via `psql -f supabase/migrations/0010_drop_rls_and_auth_uid_triggers.sql` against the live DB as the operator does for prior migrations (CLAUDE.md: applied by hand).
    3. Confirm in the DB: RLS is disabled on all 10 tables (`select relname, relrowsecurity from pg_class where relname in (...)` → all false); the `set_row_metadata`/`log_audit_event` triggers and the 5 helper functions no longer exist.
    4. POST-PUSH WRITE CHECK (the critical false-positive guard): perform one real domain write through the app (create a match) and confirm `audit_log.changed_by` is non-NULL and `created_by`/`updated_by` are set — proving app-side stamping holds NOW THAT the triggers are gone.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms RLS disabled on all 10 tables; triggers + helper functions dropped.
    - A post-push write records non-NULL `audit_log.changed_by` and stamped `created_by`/`updated_by` (app-side stamping confirmed working without triggers).
    - `handle_new_user`/`on_auth_user_created` still present (untouched).
  </acceptance_criteria>
  <resume-signal>Type "applied + verified" (RLS off, triggers gone, post-push write stamped) or describe the failure</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Final phase gate + document the accepted PostgREST window</name>
  <read_first>
    - package.json (check script)
    - .planning/phases/02-rls-removal-guard-coverage-audit/02-RESEARCH.md (Pitfall 6 lines 428-431 — accepted PostgREST exposure window)
  </read_first>
  <action>
    Run `npm run check` (lint + typecheck + test + build) — must be green post-migration. In the SUMMARY, explicitly document the accepted residual: per D-01, between this teardown and Supabase Auth retirement (Phase 5/6), any authenticated user can hit `/rest/v1/<table>` directly, bypassing app guards — a conscious, time-bounded tradeoff closed at cutover. Also note the operational follow-up for the user: rotate the existing Gemini key (deferred per CONTEXT).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 after the migration is applied.
    - SUMMARY documents the accepted PostgREST window (D-01) and the Gemini-key-rotation follow-up.
  </acceptance_criteria>
  <done>Phase verified green post-teardown; accepted residual + follow-ups documented for the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any authenticated user → PostgREST /rest/v1/* | After RLS drop, direct data-API access bypasses app guards (accepted, time-bounded). |
| live DB schema → app writes | Triggers gone; app stamping is now the sole actor-attribution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure / EoP | direct PostgREST access after RLS drop | accept | D-01 accepted, time-bounded window for an internal staff tool; closed at cutover (Phase 5/6) when Supabase Auth + its JWTs retire. Documented in Task 4 — NOT mitigated this phase. |
| T-02-09 | Repudiation | actor attribution after triggers drop | mitigate | Task 3 post-push write check confirms app-side stamping (plan 04) produces non-NULL changed_by without triggers (D-02/AUTHZ-03). |
| T-02-15 | Tampering | applying the wrong/over-broad drop (e.g. dropping handle_new_user or columns) | mitigate | Task 2 verify asserts handle_new_user + columns are NOT dropped; drop inventory matches verified RESEARCH list. |
</threat_model>

<verification>
- 0010 migration file matches the RESEARCH drop inventory; does not drop the protected objects.
- Operator confirms RLS disabled on all 10 tables + triggers/helper fns dropped after `supabase db push`.
- Post-push write records non-NULL `audit_log.changed_by`.
- `npm run check` exits 0 post-migration.
</verification>

<success_criteria>
- RLS dropped + disabled on every domain table (D-01); Supabase is plain Postgres (AUTHZ-02).
- `auth.uid()` triggers + helper functions dropped (D-03); protected objects untouched.
- App-side stamping confirmed working WITHOUT triggers (non-NULL changed_by post-push).
- Accepted PostgREST window + Gemini-key rotation documented for the user.
</success_criteria>

<output>
Create `.planning/phases/02-rls-removal-guard-coverage-audit/02-06-SUMMARY.md` when done. Confirm the four ROADMAP success criteria are met and the phase is ready for `/gsd-verify-work`.
</output>
